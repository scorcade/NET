<Window x:Class="WpfApp8.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp8"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <!--<Window.Resources>
        <RoutedUICommand x:Key="cmdRole"/>
    </Window.Resources>
    <Window.CommandBindings>
        <CommandBinding Command="{StaticResource cmdRole}" Executed="MenuItem_Click" />
    </Window.CommandBindings>

    <Window.InputBindings>
        <KeyBinding Command="{StaticResource cmdRole}" Gesture="Ctrl+R"/>
    </Window.InputBindings>-->

    <Grid>
        <RichTextBox x:Name="richTextBox" HorizontalAlignment="Left" Height="126" Margin="177,104,0,0" VerticalAlignment="Top" Width="409">
            <FlowDocument>
                <Paragraph>
                    <Run Text="https://www.baidu.com/"/>
                </Paragraph>
            </FlowDocument>
        </RichTextBox>
    </Grid>
</Window>
